import { Component, ViewEncapsulation } from '@angular/core';

@Component({
  selector: 'site-styles',
  template: ``,
  styleUrls: ['./site-styles.component.scss'],
  encapsulation: ViewEncapsulation.None
})
export class SiteStyles { }
